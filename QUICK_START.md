# 🚀 Быстрый старт - MarketPro Dashboard

## 📋 Для работы на новом ПК

### Вариант 1: GitHub Codespaces (Рекомендуется) ⭐
1. Перейдите на https://github.com/IceCHerenOK/marketpro-dashboard
2. Нажмите зеленую кнопку **"Code"** → **"Codespaces"** → **"Create codespace"**
3. Дождитесь автоматической установки зависимостей
4. Проект автоматически запустится на портах 3001 (API) и 5173 (Frontend)

### Вариант 2: Локальная установка
```bash
# 1. Клонирование репозитория
git clone https://github.com/IceCHerenOK/marketpro-dashboard.git
cd marketpro-dashboard

# 2. Установка зависимостей
npm install

# 3. Настройка окружения
cp .env.example .env
# Отредактируйте .env файл при необходимости

# 4. Запуск проекта
npm run dev
```

## 🔑 Доступ к приложению

- **Frontend**: http://localhost:5173
- **Backend API**: http://localhost:3001
- **Email**: chubarov.a@azotstore.ru
- **Пароль**: icekenrok446

## 🛠 Доступные команды

```bash
# Разработка
npm run dev              # Запуск frontend + backend
npm run dev:frontend     # Только frontend
npm run dev:backend      # Только backend

# Сборка
npm run build           # Полная сборка
npm run build:frontend  # Сборка frontend
npm run build:backend   # Сборка backend

# Electron
npm run electron:dev    # Запуск Electron в режиме разработки
npm run dist:win        # Сборка Windows приложения
```

## 📁 Структура проекта

```
marketpro-dashboard/
├── src/
│   ├── frontend/       # React приложение (порт 5173)
│   ├── backend/        # Node.js API (порт 3001)
│   └── electron/       # Electron main процесс
├── dist/              # Собранные файлы
├── data/              # SQLite база данных
└── release/           # Electron сборки (.exe файлы)
```

## 🔧 Настройка API маркетплейсов

1. Войдите в приложение
2. Перейдите в **"Настройки"** → **"Маркетплейсы"**
3. Выберите нужный маркетплейс (Wildberries, OZON, и т.д.)
4. Введите API ключи
5. Нажмите **"Тест подключения"**
6. Сохраните настройки

## 🐛 Решение проблем

### Ошибки портов
```bash
# Если порты заняты, измените в .env файле:
PORT=3002  # Вместо 3001
```

### Проблемы с зависимостями
```bash
# Очистка и переустановка
rm -rf node_modules package-lock.json
npm install
```

### Ошибки базы данных
```bash
# Пересоздание БД
rm -rf data/
npm run dev  # БД создастся автоматически
```

## 📞 Поддержка

- **GitHub**: https://github.com/IceCHerenOK/marketpro-dashboard
- **Issues**: https://github.com/IceCHerenOK/marketpro-dashboard/issues

---

**Готово!** Теперь вы можете продолжить разработку на любом ПК 🎉