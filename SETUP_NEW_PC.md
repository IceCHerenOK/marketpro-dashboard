# 🚀 Инструкция по развертыванию проекта на новом ПК

## 📋 Предварительные требования

### 1. Установка необходимого ПО
- **Node.js** (версия 18 или выше) - https://nodejs.org/
- **Git** - https://git-scm.com/download/win
- **Kiro IDE** - для разработки

### 2. Проверка установки
```bash
node --version
npm --version
git --version
```

## 📦 Развертывание проекта

### 1. Клонирование репозитория
```bash
# Клонирование проекта с GitHub
git clone https://github.com/IceCHerenOK/marketpro-dashboard.git
cd marketpro-dashboard
```

### 2. Установка зависимостей
```bash
npm install
```

### 3. Настройка окружения
Создайте файл `.env` в корне проекта:
```env
# Порт для backend сервера
PORT=3001

# JWT секрет для аутентификации
JWT_SECRET=your-super-secret-jwt-key-here

# База данных
DATABASE_PATH=./data/marketplace.db

# API ключи маркетплейсов (заполните своими)
WILDBERRIES_API_KEY=
OZON_CLIENT_ID=
OZON_API_KEY=
YANDEX_MARKET_TOKEN=
MEGAMARKET_API_KEY=
MAGNITMARKET_API_KEY=

# Настройки приложения
APP_NAME=MarketPro
APP_VERSION=1.0.0
```

### 4. Инициализация базы данных
```bash
# Создание структуры БД и тестовых данных
npm run init-db
```

## 🏃‍♂️ Запуск проекта

### Режим разработки
```bash
# Запуск всего приложения (frontend + backend)
npm run dev

# Или запуск компонентов по отдельности:
npm run dev:frontend  # Только frontend (порт 5173)
npm run dev:backend   # Только backend (порт 3001)
npm run electron:dev  # Electron приложение
```

### Сборка для продакшена
```bash
# Полная сборка
npm run build

# Сборка Electron приложения
npm run dist:win
```

## 🔧 Настройка в Kiro IDE

### 1. Открытие проекта
1. Запустите Kiro
2. File → Open Folder
3. Выберите папку с проектом

### 2. Настройка задач
Kiro автоматически определит npm скрипты из `package.json`

### 3. Отладка
- Frontend: http://localhost:5173
- Backend API: http://localhost:3001
- Electron: запускается отдельным процессом

## 🔑 Первый запуск

### 1. Учетные данные по умолчанию
- **Email**: `chubarov.a@azotstore.ru`
- **Пароль**: `icekenrok446`

### 2. Настройка API ключей
1. Войдите в приложение
2. Перейдите в "Настройки" → "Маркетплейсы"
3. Добавьте API ключи для нужных маркетплейсов
4. Протестируйте подключение

## 📁 Структура проекта

```
marketpro-dashboard/
├── src/
│   ├── frontend/          # React приложение
│   ├── backend/           # Node.js API сервер
│   └── electron/          # Electron main процесс
├── dist/                  # Собранные файлы
├── release/               # Electron сборки
├── data/                  # База данных SQLite
└── assets/                # Иконки и ресурсы
```

## 🐛 Решение проблем

### Ошибки установки зависимостей
```bash
# Очистка кэша npm
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

### Проблемы с портами
- Frontend по умолчанию: 5173
- Backend по умолчанию: 3001
- Измените в файлах конфигурации при конфликтах

### Ошибки базы данных
```bash
# Пересоздание БД
rm data/marketplace.db
npm run init-db
```

## 🔄 Синхронизация изменений

### Получение обновлений
```bash
git pull origin main
npm install  # Если изменились зависимости
```

### Отправка изменений
```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера (F12)
2. Проверьте логи backend сервера
3. Убедитесь, что все зависимости установлены
4. Проверьте правильность API ключей

---

**Готово!** Теперь вы можете продолжить разработку на новом ПК 🎉